# Python 3.13 + Apple Silicon (M-series) compatible
# tensorflow-model-optimization 0.7.5/0.8.0 pins numpy~=1.23, which conflicts
# with TF 2.13+ on Python 3.13 (needs numpy>=2.1.0).
# Solution: use tf-keras standalone + tfmot from source, or use the workaround below.
#
# Recommended: create env with Python 3.11 to avoid the numpy clash:
#   conda create -n tinyml-audio python=3.11
#   conda activate tinyml-audio
#   pip install -r requirements.txt
#
# On Python 3.11 these versions are fully compatible:
tensorflow==2.15.0
tensorflow-model-optimization==0.7.5
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
librosa>=0.10.0
soundfile>=0.12.1
matplotlib>=3.7.0
scikit-learn>=1.3.0
tqdm>=4.65.0
